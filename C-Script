-- [[ Put this LocalScript in StarterPlayer -> StarterPlayerScripts ]] --

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LP = Players.LocalPlayer

local isFlying = false
local bodyVelocity, bodyGyro = nil, nil
local flySpeed = 60

-- ==================== [ SYSTEM FUNCTIONS ] ====================

-- 1. ระบบบิน / ลอย
local function setFly(state, speed)
	if speed then flySpeed = speed end
	local char = LP.Character
	if not char or not char:FindFirstChild("HumanoidRootPart") then return end
	local hrp = char.HumanoidRootPart
	local hum = char:FindFirstChildOfClass("Humanoid")

	if state then
		if isFlying then return end
		isFlying = true
		if hum then hum.PlatformStand = true end

		bodyVelocity = Instance.new("BodyVelocity", hrp)
		bodyVelocity.MaxForce = Vector3.new(1, 1, 1) * 100000
		bodyVelocity.Velocity = Vector3.zero

		bodyGyro = Instance.new("BodyGyro", hrp)
		bodyGyro.MaxTorque = Vector3.new(1, 1, 1) * 100000
		bodyGyro.CFrame = hrp.CFrame

		task.spawn(function()
			while isFlying and char and hrp do
				local camera = workspace.CurrentCamera
				local moveDir = hum and hum.MoveDirection or Vector3.zero
				if moveDir.Magnitude > 0 then
					bodyVelocity.Velocity = camera.CFrame:VectorToWorldSpace(Vector3.new(moveDir.X, 0, moveDir.Z)) * flySpeed
				else
					bodyVelocity.Velocity = Vector3.zero
				end
				bodyGyro.CFrame = camera.CFrame
				RunService.RenderStepped:Wait()
			end
		end)
	else
		isFlying = false
		if hum then hum.PlatformStand = false end
		if bodyVelocity then bodyVelocity:Destroy() bodyVelocity = nil end
		if bodyGyro then bodyGyro:Destroy() bodyGyro = nil end
	end
end

-- 2. ระบบสร้างหน้าต่าง Buy Item ปลอม (Fake Purchase Prompt)
local function createFakeBuyPrompt(robuxAmount)
	local playerGui = LP:WaitForChild("PlayerGui")
	
	local existing = playerGui:FindFirstChild("FakeBuyPromptGui")
	if existing then existing:Destroy() end

	local formattedAmount = tostring(robuxAmount):reverse():gsub("(%d%d%d)", "%1,"):reverse():gsub("^,", "")

	local screenGui = Instance.new("ScreenGui")
	screenGui.Name = "FakeBuyPromptGui"
	screenGui.ResetOnSpawn = false
	screenGui.Parent = playerGui

	-- ฉากหลังมืดลง
	local bgDark = Instance.new("Frame")
	bgDark.Size = UDim2.new(1, 0, 1, 0)
	bgDark.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	bgDark.BackgroundTransparency = 0.5
	bgDark.Parent = screenGui

	-- กรอบ Buy item
	local promptFrame = Instance.new("Frame")
	promptFrame.Size = UDim2.new(0, 420, 0, 250)
	promptFrame.Position = UDim2.new(0.5, -210, 0.5, -125)
	promptFrame.BackgroundColor3 = Color3.fromRGB(24, 25, 28)
	promptFrame.Parent = screenGui
	Instance.new("UICorner", promptFrame).CornerRadius = UDim.new(0, 12)

	-- หัวเรื่อง (Buy item)
	local titleLabel = Instance.new("TextLabel")
	titleLabel.Size = UDim2.new(0, 150, 0, 30)
	titleLabel.Position = UDim2.new(0, 20, 0, 15)
	titleLabel.BackgroundTransparency = 1
	titleLabel.Text = "Buy item"
	titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	titleLabel.Font = Enum.Font.BuilderSansBold
	titleLabel.TextSize = 20
	titleLabel.TextXAlignment = Enum.TextXAlignment.Left
	titleLabel.Parent = promptFrame

	-- ปุ่มปิด (X)
	local closeBtn = Instance.new("TextButton")
	closeBtn.Size = UDim2.new(0, 30, 0, 30)
	closeBtn.Position = UDim2.new(1, -40, 0, 12)
	closeBtn.BackgroundTransparency = 1
	closeBtn.Text = "✕"
	closeBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
	closeBtn.Font = Enum.Font.SourceSansBold
	closeBtn.TextSize = 18
	closeBtn.Parent = promptFrame

	closeBtn.MouseButton1Click:Connect(function()
		screenGui:Destroy()
	end)

	-- ยอด Robux ปลอม (มุมขวาบนตรงเลข 0)
	local robuxFrame = Instance.new("Frame")
	robuxFrame.Size = UDim2.new(0, 120, 0, 30)
	robuxFrame.Position = UDim2.new(1, -160, 0, 12)
	robuxFrame.BackgroundTransparency = 1
	robuxFrame.Parent = promptFrame

	local rIcon = Instance.new("ImageLabel")
	rIcon.Size = UDim2.new(0, 18, 0, 18)
	rIcon.Position = UDim2.new(1, -22, 0.5, -9)
	rIcon.BackgroundTransparency = 1
	rIcon.Image = "rbxassetid://9120612143"
	rIcon.Parent = robuxFrame

	local rText = Instance.new("TextLabel")
	rText.Size = UDim2.new(1, -28, 1, 0)
	rText.Position = UDim2.new(0, 0, 0, 0)
	rText.BackgroundTransparency = 1
	rText.Text = formattedAmount
	rText.TextColor3 = Color3.fromRGB(255, 255, 255)
	rText.Font = Enum.Font.BuilderSansBold
	rText.TextSize = 16
	rText.TextXAlignment = Enum.TextXAlignment.Right
	rText.Parent = robuxFrame

	-- ไอคอนสินค้า (Boombox)
	local itemIcon = Instance.new("ImageLabel")
	itemIcon.Size = UDim2.new(0, 60, 0, 60)
	itemIcon.Position = UDim2.new(0, 20, 0, 55)
	itemIcon.BackgroundColor3 = Color3.fromRGB(35, 37, 42)
	itemIcon.Image = "rbxassetid://21268181" -- รูป Boombox
	itemIcon.Parent = promptFrame
	Instance.new("UICorner", itemIcon).CornerRadius = UDim.new(0, 8)

	-- ชื่อสินค้า
	local itemName = Instance.new("TextLabel")
	itemName.Size = UDim2.new(0, 200, 0, 25)
	itemName.Position = UDim2.new(0, 90, 0, 60)
	itemName.BackgroundTransparency = 1
	itemName.Text = "Boombox 🎵"
	itemName.TextColor3 = Color3.fromRGB(255, 255, 255)
	itemName.Font = Enum.Font.BuilderSansBold
	itemName.TextSize = 16
	itemName.TextXAlignment = Enum.TextXAlignment.Left
	itemName.Parent = promptFrame

	-- ราคาสินค้า
	local priceIcon = Instance.new("ImageLabel")
	priceIcon.Size = UDim2.new(0, 14, 0, 14)
	priceIcon.Position = UDim2.new(0, 90, 0, 90)
	priceIcon.BackgroundTransparency = 1
	priceIcon.Image = "rbxassetid://9120612143"
	priceIcon.Parent = promptFrame

	local priceLabel = Instance.new("TextLabel")
	priceLabel.Size = UDim2.new(0, 100, 0, 20)
	priceLabel.Position = UDim2.new(0, 108, 0, 87)
	priceLabel.BackgroundTransparency = 1
	priceLabel.Text = "849"
	priceLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	priceLabel.Font = Enum.Font.BuilderSans
	priceLabel.TextSize = 14
	priceLabel.TextXAlignment = Enum.TextXAlignment.Left
	priceLabel.Parent = promptFrame

	-- ปุ่ม Buy
	local buyBtn = Instance.new("TextButton")
	buyBtn.Size = UDim2.new(1, -40, 0, 42)
	buyBtn.Position = UDim2.new(0, 20, 0, 130)
	buyBtn.BackgroundColor3 = Color3.fromRGB(0, 110, 255)
	buyBtn.Text = "Buy"
	buyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	buyBtn.Font = Enum.Font.BuilderSansBold
	buyBtn.TextSize = 16
	buyBtn.Parent = promptFrame
	Instance.new("UICorner", buyBtn).CornerRadius = UDim.new(0, 8)

	buyBtn.MouseButton1Click:Connect(function()
		screenGui:Destroy()
	end)

	-- แถบ Roblox Plus ด้านล่าง
	local subFrame = Instance.new("Frame")
	subFrame.Size = UDim2.new(1, -40, 0, 40)
	subFrame.Position = UDim2.new(0, 20, 0, 185)
	subFrame.BackgroundColor3 = Color3.fromRGB(15, 16, 18)
	subFrame.Parent = promptFrame
	Instance.new("UICorner", subFrame).CornerRadius = UDim.new(0, 8)

	local subText = Instance.new("TextLabel")
	subText.Size = UDim2.new(0.7, 0, 1, 0)
	subText.Position = UDim2.new(0, 12, 0, 0)
	subText.BackgroundTransparency = 1
	subText.Text = "Get 10% off with Roblox Plus"
	subText.TextColor3 = Color3.fromRGB(200, 200, 200)
	subText.Font = Enum.Font.BuilderSans
	subText.TextSize = 12
	subText.TextXAlignment = Enum.TextXAlignment.Left
	subText.Parent = subFrame

	local newBadge = Instance.new("TextLabel")
	newBadge.Size = UDim2.new(0, 45, 0, 22)
	newBadge.Position = UDim2.new(1, -55, 0.5, -11)
	newBadge.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	newBadge.Text = "New"
	newBadge.TextColor3 = Color3.fromRGB(0, 0, 0)
	newBadge.Font = Enum.Font.BuilderSansBold
	newBadge.TextSize = 11
	newBadge.Parent = subFrame
	Instance.new("UICorner", newBadge).CornerRadius = UDim.new(0, 11)
end

-- Variables สำหรับระบบ UI แชทส่วนตัว
local addChatMessage = function(sender, text, color) end

-- ==================== [ AI DYNAMIC PROCESSOR ] ====================

local function processAICommand(msg)
	local text = string.lower(msg)
	local char = LP.Character
	if not char then return end
	local hum = char:FindFirstChildOfClass("Humanoid")
	local hrp = char:FindFirstChild("HumanoidRootPart")

	-- ดึงตัวเลขแรกที่พบในประโยค (ถ้ามี)
	local numberInput = tonumber(string.match(text, "%d+%.?%d*"))

	-- A. หมวดเสก Robux ปลอม (แสดงบนหน้าต่าง Buy item)
	if string.find(text, "โรบัค") or string.find(text, "โรบัก") or string.find(text, "robux") or string.find(text, "จำลอง") then
		local amount = numberInput or 999999
		createFakeBuyPrompt(amount)
		addChatMessage("AI", "เปิดหน้าต่าง Buy item พร้อมกับเสก Robux จำนวน " .. amount .. " R$ ให้แล้วครับ!", Color3.fromRGB(0, 255, 127))

	-- B. หมวดการบิน / ลอย / ตก
	elseif string.find(text, "บิน") or string.find(text, "ลอย") or string.find(text, "เหาะ") then
		if string.find(text, "หยุด") or string.find(text, "เลิก") or string.find(text, "ปิด") or string.find(text, "ลง") then
			setFly(false)
			addChatMessage("AI", "ยกเลิกการบินเรียบร้อยครับ", Color3.fromRGB(255, 200, 100))
		else
			local spd = numberInput or 60
			setFly(true, spd)
			addChatMessage("AI", "เปิดโหมดบินความเร็ว " .. spd .. " เรียบร้อยครับ", Color3.fromRGB(100, 255, 100))
		end

	-- C. หมวดความเร็ว (วิ่ง / เดิน / สปีด)
	elseif string.find(text, "วิ่ง") or string.find(text, "เดิน") or string.find(text, "สปีด") or string.find(text, "เร็ว") or string.find(text, "ไว") then
		if string.find(text, "ปกติ") or string.find(text, "ช้า") then
			if hum then hum.WalkSpeed = 16 end
			addChatMessage("AI", "ปรับความเร็วกลับเป็นปกติ (16) แล้วครับ", Color3.fromRGB(255, 200, 100))
		else
			local spd = numberInput or 100
			if hum then hum.WalkSpeed = spd end
			addChatMessage("AI", "ปรับความเร็ววิ่งเป็น " .. spd .. " เรียบร้อยครับ", Color3.fromRGB(100, 255, 100))
		end

	-- D. หมวดการกระโดด (โดด / สูง)
	elseif string.find(text, "โดด") or string.find(text, "เด้ง") then
		if string.find(text, "ปกติ") then
			if hum then hum.JumpPower = 50 end
			addChatMessage("AI", "ปรับแรงกระโดดกลับเป็นปกติ (50) แล้วครับ", Color3.fromRGB(255, 200, 100))
		else
			local jp = numberInput or 150
			if hum then hum.UseJumpPower = true hum.JumpPower = jp end
			addChatMessage("AI", "ปรับแรงกระโดดเป็น " .. jp .. " เรียบร้อยครับ", Color3.fromRGB(100, 255, 100))
		end

	-- E. หมวดขนาดตัวละคร (ใหญ่ / เล็ก / ย่อ / ขยาย)
	elseif string.find(text, "ใหญ่") or string.find(text, "ยักษ์") or string.find(text, "เล็ก") or string.find(text, "ย่อ") or string.find(text, "ตัว") then
		local scale = numberInput or (string.find(text, "เล็ก") and 0.5 or 2)
		if hum then
			for _, prop in ipairs({"BodyDepthScale", "BodyHeightScale", "BodyWidthScale", "HeadScale"}) do
				local s = hum:FindFirstChild(prop)
				if s then s.Value = scale end
			end
		end
		addChatMessage("AI", "ปรับขนาดตัวละครเป็นขนาด " .. scale .. " เท่าเรียบร้อยครับ", Color3.fromRGB(100, 255, 100))

	-- F. หมวดวาร์ป / ย้ายตำแหน่ง (ขึ้นฟ้า / ไปข้างหน้า / วาร์ป)
	elseif string.find(text, "วาป") or string.find(text, "วาร์ป") or string.find(text, "ไป") or string.find(text, "ขึ้น") then
		local dist = numberInput or 50
		if hrp then
			if string.find(text, "ฟ้า") or string.find(text, "สูง") or string.find(text, "บน") then
				hrp.CFrame = hrp.CFrame + Vector3.new(0, dist, 0)
				addChatMessage("AI", "วาร์ปขึ้นฟ้า " .. dist .. " หน่วยเรียบร้อยครับ", Color3.fromRGB(100, 255, 100))
			else
				hrp.CFrame = hrp.CFrame + (hrp.CFrame.LookVector * dist)
				addChatMessage("AI", "วาร์ปไปข้างหน้า " .. dist .. " หน่วยเรียบร้อยครับ", Color3.fromRGB(100, 255, 100))
			end
		end

	-- G. หมวดมองทะลุ / ลอยทะลุตึก (Noclip)
	elseif string.find(text, "ทะลุ") then
		for _, part in ipairs(char:GetChildren()) do
			if part:IsA("BasePart") then part.CanCollide = false end
		end
		addChatMessage("AI", "เปิดโหมดเดินทะลุสิ่งกีดขวางแล้วครับ", Color3.fromRGB(100, 255, 100))

	-- H. หมวดรีเซ็ต / กลับเป็นปกติ
	elseif string.find(text, "รี") or string.find(text, "ปกติ") or string.find(text, "เดิม") then
		setFly(false)
		if hum then
			hum.WalkSpeed = 16
			hum.JumpPower = 50
			for _, prop in ipairs({"BodyDepthScale", "BodyHeightScale", "BodyWidthScale", "HeadScale"}) do
				local s = hum:FindFirstChild(prop)
				if s then s.Value = 1 end
			end
		end
		addChatMessage("AI", "รีเซ็ตสถานะตัวละครกลับเป็นปกติเรียบร้อยครับ", Color3.fromRGB(255, 200, 100))
	else
		addChatMessage("AI", "ฉันไม่เข้าใจคำสั่งนี้ ลองสั่งว่า 'ขอโรบัค 50000', 'บิน', 'วิ่งเร็ว 100' ดูสิครับ", Color3.fromRGB(255, 100, 100))
	end
end

-- ==================== [ PRIVATE AI CHAT GUI ] ====================

local function createPrivateChatUI()
	local playerGui = LP:WaitForChild("PlayerGui")
	local existing = playerGui:FindFirstChild("PrivateAIChatGui")
	if existing then existing:Destroy() end

	local screenGui = Instance.new("ScreenGui")
	screenGui.Name = "PrivateAIChatGui"
	screenGui.ResetOnSpawn = false
	screenGui.Parent = playerGui

	-- กรอบหลักของกล่องแชท
	local mainFrame = Instance.new("Frame")
	mainFrame.Name = "MainFrame"
	mainFrame.Size = UDim2.new(0, 320, 0, 240)
	mainFrame.Position = UDim2.new(0, 20, 0.5, -120)
	mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	mainFrame.BackgroundTransparency = 0.15
	mainFrame.ClipsDescendants = true
	mainFrame.Parent = screenGui
	Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 10)

	local uiStroke = Instance.new("UIStroke", mainFrame)
	uiStroke.Color = Color3.fromRGB(0, 170, 255)
	uiStroke.Thickness = 1.5

	-- แถบหัวเรื่อง (Title Bar)
	local titleBar = Instance.new("TextLabel")
	titleBar.Size = UDim2.new(1, 0, 0, 30)
	titleBar.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
	titleBar.Text = "  🤖 Private AI Assistant"
	titleBar.TextColor3 = Color3.fromRGB(255, 255, 255)
	titleBar.Font = Enum.Font.FredokaOne
	titleBar.TextSize = 14
	titleBar.TextXAlignment = Enum.TextXAlignment.Left
	titleBar.Parent = mainFrame

	-- ช่องแสดงประวัติแชท (ScrollingFrame)
	local scrollFrame = Instance.new("ScrollingFrame")
	scrollFrame.Size = UDim2.new(0.94, 0, 0.65, 0)
	scrollFrame.Position = UDim2.new(0.03, 0, 0.15, 0)
	scrollFrame.BackgroundTransparency = 1
	scrollFrame.ScrollBarThickness = 4
	scrollFrame.Parent = mainFrame

	local uiListLayout = Instance.new("UIListLayout")
	uiListLayout.SortOrder = Enum.SortOrder.LayoutOrder
	uiListLayout.Padding = UDim.new(0, 5)
	uiListLayout.Parent = scrollFrame

	-- ช่องพิมพ์ข้อความ (TextBox)
	local inputFrame = Instance.new("Frame")
	inputFrame.Size = UDim2.new(0.94, 0, 0.15, 0)
	inputFrame.Position = UDim2.new(0.03, 0, 0.82, 0)
	inputFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
	inputFrame.Parent = mainFrame
	Instance.new("UICorner", inputFrame).CornerRadius = UDim.new(0, 6)

	local textBox = Instance.new("TextBox")
	textBox.Size = UDim2.new(0.75, 0, 1, 0)
	textBox.Position = UDim2.new(0.03, 0, 0, 0)
	textBox.BackgroundTransparency = 1
	textBox.PlaceholderText = "พิมพ์สั่ง AI ที่นี่..."
	textBox.Text = ""
	textBox.TextColor3 = Color3.fromRGB(255, 255, 255)
	textBox.Font = Enum.Font.SourceSans
	textBox.TextSize = 14
	textBox.TextXAlignment = Enum.TextXAlignment.Left
	textBox.Parent = inputFrame

	local sendBtn = Instance.new("TextButton")
	sendBtn.Size = UDim2.new(0.2, 0, 0.8, 0)
	sendBtn.Position = UDim2.new(0.78, 0, 0.1, 0)
	sendBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
	sendBtn.Text = "ส่ง"
	sendBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	sendBtn.Font = Enum.Font.FredokaOne
	sendBtn.TextSize = 13
	sendBtn.Parent = inputFrame
	Instance.new("UICorner", sendBtn).CornerRadius = UDim.new(0, 4)

	-- ฟังก์ชั่นเพิ่มข้อความเข้าในกล่องแชท
	addChatMessage = function(sender, text, color)
		local msgLabel = Instance.new("TextLabel")
		msgLabel.Size = UDim2.new(1, 0, 0, 0)
		msgLabel.BackgroundTransparency = 1
		msgLabel.Text = "<b>" .. sender .. ":</b> " .. text
		msgLabel.RichText = true
		msgLabel.TextColor3 = color or Color3.fromRGB(255, 255, 255)
		msgLabel.Font = Enum.Font.SourceSans
		msgLabel.TextSize = 14
		msgLabel.TextWrapped = true
		msgLabel.TextXAlignment = Enum.TextXAlignment.Left
		msgLabel.Parent = scrollFrame

		-- ปรับขนาดความสูงตามข้อความอัตโนมัติ
		msgLabel.Size = UDim2.new(1, 0, 0, msgLabel.TextBounds.Y + 2)
		scrollFrame.CanvasSize = UDim2.new(0, 0, 0, uiListLayout.AbsoluteContentSize.Y + 10)
		scrollFrame.CanvasPosition = Vector2.new(0, scrollFrame.CanvasSize.Y.Offset)
	end

	-- ระบบการส่งข้อความ
	local function onSend()
		local text = textBox.Text
		if text ~= "" then
			textBox.Text = ""
			addChatMessage("คุณ", text, Color3.fromRGB(200, 220, 255))
			processAICommand(text)
		end
	end

	sendBtn.MouseButton1Click:Connect(onSend)
	textBox.FocusLost:Connect(function(enterPressed)
		if enterPressed then onSend() end
	end)

	-- ระบบลากกล่องแชทส่วนตัวย้ายตำแหน่ง (Draggable UI)
	local dragging, dragInput, dragStart, startPos
	titleBar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = mainFrame.Position
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then dragging = false end
			end)
		end
	end)
	titleBar.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)

	-- ข้อความต้อนรับเมื่อเปิดเกม
	addChatMessage("AI", "สวัสดีครับ! พิมพ์สั่ง AI ได้เลย เช่น 'ขอโรบัค 50000', 'บิน', 'วิ่งเร็ว 100'", Color3.fromRGB(150, 220, 255))
end

-- ==================== [ EXECUTION ] ====================

LP.CharacterAdded:Connect(function() setFly(false) end)
createPrivateChatUI()
